<% provide(:title, @user.name) %>
    <div class="row">
        <aside class="col-md-4">
            <section class="user_info">
                <h1>
                <%= gravatar_for @user %>
                <%= @user.name %>
                </h1>
            </section>
        </aside>
    </div>

    <div>
    <p> Use the pull-down <span class="strong">Questions</span> menu in the navbar to display and/or edit questions. 

    </div>